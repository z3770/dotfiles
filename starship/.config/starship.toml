# Get editor completions based on the config schema
"$schema" = 'https://starship.rs/config-schema.json'

# Inserts a blank line between shell prompts
add_newline = false
right_format = """$time"""

# Replace the '❯' symbol in the prompt with '➜'
[character] # The name of the module we are configuring is 'character'
success_symbol = '[➜](bold blue)' # The 'success_symbol' segment is being set to '➜' with the color 'bold green'
# Keep default left prompt, but add time on the right side

# Configure how the time looks
[time]
disabled = false
time_format = "%T"          # HH:MM:SS (24-hour) → change to "%R" for HH:MM only
format = '[$time](bold blue)'   # you can change emoji, color, etc.
